{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm turbo run build --filter=@autobureau/web...",
  "outputDirectory": ".next",
  "regions": ["iad1"],
  "github": {
    "enabled": false
  }
}
