# Plaid Sandbox checkpoint — September 13, 2026

The signed-in dashboard identifies the team as **Pellum** and exposes Sandbox setup. Production access remains an uncompleted request, with a security questionnaire required. Transactions, Liabilities and Balance appear in the product catalog; this is not proof of API product enablement or Production approval. No Enable Sandbox, Try for free/real-data flow, Production access request or financial Link flow was submitted. No provider secret was revealed, copied or distributed.

Repository foundation now provides a closed Sandbox configuration guard, server-side Link token creation/update mode, one-time public-token exchange, Item removal and verification-key retrieval. Link binds an opaque hash to the authenticated user/household/scope inputs; the future HTTP boundary must supply those inputs from its verified principal. There is no HTTP route or UI integration yet. Link requests only Transactions with optional Liabilities; this does not prove balance/transaction/liability ingestion.

The client has one ten-second request attempt, a 64 KiB response cap, fixed Sandbox origin, no redirects/caching and coarse sanitized errors. A caller must reconcile ambiguous exchange/removal rather than blindly retrying. Production configuration, tokens, misplaced staging identifiers and public secret variables fail closed. No public endpoint is enabled.

Webhook verification uses ES256, the fetched exact JWK ID, key validity, a five-minute JWT age bound and SHA-256 comparison over the untouched body before JSON parsing. It rejects future/expired/mismatched claims and Production notices, caches a bounded set of public keys and strips all fields except opaque routing metadata. It does not use a body hash as a permanent logical-event identity: repeated unchanged provider notices may still require a fresh authoritative sync. Durable processing/idempotency remains to be implemented.

41 focused client/signature tests pass; 185 affected Plaid/observability tests pass after expanding log scrubbing to access/public/link tokens across all provider environments. These are local tests using synthetic keys/responses, not actual Sandbox API lifecycle proof.

Required remaining work: reviewed encrypted token custody separate from identifier-grade secrets, Item/institution/account persistence, balances/transactions/liabilities projection, consent/UI and authenticated routes, strict customer/household ownership, duplicate Item control, webhook routing/durable effects, cursor/retry/reconciliation, reconnect/unlink and verifiable privacy deletion. Then configure only Sandbox secrets through approved server custody and execute actual provider lifecycle and final stable-staging tests. No real financial data or Plaid Production is authorized. No Production readiness is inferred from this foundation.

Sources: [Link API](https://plaid.com/docs/api/link/), [Item API](https://plaid.com/docs/api/items/), [webhook verification](https://plaid.com/docs/api/webhooks/webhook-verification/). Current product scope is [PRD §21.1](../product/PRD-v1.md); the old August architecture audit's Plaid deferral is superseded by that amendment.
